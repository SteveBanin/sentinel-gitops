apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: sentinel
  labels:
    app: api-service
spec:
  selector:
    app: api-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080