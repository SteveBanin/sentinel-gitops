apiVersion: v1
kind: Service
metadata:
  name: sentinel-postgres
  namespace: sentinel
  labels:
    app: sentinel-postgres
spec:
  selector:
    app: sentinel-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432