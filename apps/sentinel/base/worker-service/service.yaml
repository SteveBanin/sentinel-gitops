apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: sentinel
  labels:
    app: worker-service
spec:
  selector:
    app: worker-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081