apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api-service
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames: ["sentinel-test"]
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: worker-service
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames: ["sentinel-test"]
